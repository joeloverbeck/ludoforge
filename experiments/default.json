{
  "seed": 42,
  "runner": {
    "generations": 10000,
    "maxRetainedGenerations": 30,
    "shortlistSize": 8,
    "saveEvery": 5
  },
  "mapElites": {
    "descriptors": [
      { "id": "agency", "min": 0, "max": 1, "bins": 5 },
      { "id": "variety", "min": 0, "max": 1, "bins": 5 },
      { "id": "strategic_depth", "min": 0, "max": 50, "bins": 5 },
      { "id": "interaction_rate", "min": 0, "max": 1, "bins": 5 }
    ],
    "tieBreak": "replace"
  },
  "seeding": {
    "mode": "generate",
    "populationSize": 16,
    "generate": {
      "coverage": {
        "strategy": "uniform-bins",
        "maxAttempts": 500,
        "fallback": {
          "strategy": "accept-any-valid"
        }
      },
      "grammar": {}
    }
  }
}
