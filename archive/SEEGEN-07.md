# SEEGEN-07: Update architectural docs

**Status: COMPLETED**

## Summary

Update architecture docs to reflect the seed generation capability. Remove "no generator exists in-core" from pipeline overview. Document seeding modes, config schema, and seed-report artifact.

## Files to touch

- `docs/architecture/pipeline-overview.md` — update Stage 1 description
- `docs/architecture/evolution-runner.md` — add Seeding section

## Out of scope

- Code changes
- API/JSDoc documentation

## Acceptance criteria

### Invariants
- `pipeline-overview.md` Stage 1 reads: "Seed population (generated by default; optional folder/import seeding)" ✅
- `pipeline-overview.md` references `src/seed-generation/` as relevant code ✅
- `evolution-runner.md` has a "Seeding" section documenting the three modes (generate, folder, mixed) ✅
- `evolution-runner.md` documents `seed-report.json` artifact ✅
- `evolution-runner.md` references `runner-config.schema.json` (not `runner-config.v1.json`) ✅
- No stale references to `runner-config.v1.json` remain in any doc ✅
- All file paths mentioned in docs exist in the repo ✅

## Outcome

### What was planned
Update two architecture docs to remove stale "no generator exists in-core" language and add seeding documentation.

### What was actually changed

**`docs/architecture/pipeline-overview.md`**:
- Stage 1 rewritten from "Seed population (external input) — no generator exists in-core" to describe the three seeding modes (generate, folder, mixed) and reference `src/seed-generation/`, `seed-resolver.js`, and `folder-seeder.js`.

**`docs/architecture/evolution-runner.md`**:
- Fixed stale reference from `runner-config.v1.json` → `runner-config.schema.json` (line 65).
- Added a full "Seeding" section before "Current Implementation Status", covering: modes table, configuration keys table, seed resolution flow, artifacts (`seed-report.json`), and relevant code paths.

No code changes were required. No ticket corrections were needed — all assumptions matched the codebase.
