{
  "name": "ludoforge",
  "private": true,
  "type": "module",
  "devDependencies": {
    "ajv": "^8.12.0"
  },
  "scripts": {
    "test": "node --test test/**/*.test.mjs && tsc -p tsconfig.json"
  }
}
