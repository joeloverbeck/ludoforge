{
  "name": "ludoforge",
  "private": true,
  "type": "module",
  "devDependencies": {
    "ajv": "^8.12.0"
  },
  "scripts": {
    "test": "node --test test/dsl/schema.test.mjs && tsc -p tsconfig.json"
  }
}
